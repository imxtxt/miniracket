(ocamllex lexer)

(menhir
 (modules parser))

(library
 (name src)
 (wrapped false)
 (libraries base)
 (modules
  allocate_registers
  ast_mon
  ast
  explicate_control
  graph
  info
  inject_info
  interference_graph
  ir
  lexer
  parser
  patch_instructions
  prelude_conclusion
  remove_complex_operands
  select_instructions
  type_check
  type
  uncover_live
  uniquify
  utilities
  x86))
